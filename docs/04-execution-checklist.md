# Execution Check-Plan — что осталось сделать перед сдачей

Статус на 2026-08-04. Отмечайте `[x]` по мере выполнения.

---

## Блок 0 · Что уже сделано (проверять не нужно)

- [x] Test plan: scope, risk matrix, 24 сценария, severity/priority — `docs/02-test-plan.md`
- [x] Executive summary 200–400 слов — `docs/01-executive-summary.md`
- [x] 3 баг-репорта, **все воспроизведены вживую**, со скриншотами — `docs/03-bug-reports.md` + `docs/evidence/`
- [x] Раздел «Not reproduced» — две гипотезы честно опровергнуты
- [x] Playwright POM-фреймворк: config, 4 page-объекта, фикстуры, 2 спеки — `automation/`
- [x] `npx tsc --noEmit` — чисто; `npx playwright test --list` — 14 тестов × 2 проекта
- [x] Локаторы верифицированы против живого DOM amazon.com (см. test plan §7)

---

## Блок 1 · Прогнать автотесты локально ⚠️ ОБЯЗАТЕЛЬНО

В песочнице тесты не запускались — нет прав ставить системные библиотеки браузера (`libxdamage1`, требуется `sudo`). Компиляция и листинг тестов проверены, но **реального зелёного прогона нет**. Это единственный незакрытый пункт.

```bash
cd automation
npm install
npx playwright install chromium
npm run test:desktop        # начните с desktop
npm run report              # HTML-отчёт
```

- [ ] `npm run test:desktop` — зафиксировать pass/fail
- [ ] `npm run test:mobile` — Pixel 7
- [ ] Если тест падает — **чинить локатор, а не удалять тест**; частые причины ниже
- [ ] Сохранить `playwright-report/` или скриншот сводки как evidence в `docs/evidence/`
- [ ] Обновить `README.md`: заменить «tsc чисто, 14 тестов перечисляются» на реальный результат прогона

**Ожидаемые причины падений и что делать**

| Симптом | Причина | Действие |
|---|---|---|
| `Amazon served a CAPTCHA interstitial` | Анти-бот | `npm run test:headed`, пройти капчу руками, перезапустить |
| PDP: нет `#add-to-cart-button` | Локация доставки = не-США → товар не отгружается (см. BUG-003) | Сбросить delivery location на US ZIP перед прогоном |
| Мобильный проект падает на корзине | Мобильная вёрстка корзины отличается | Добавить мобильный фолбэк-локатор в `CartPage` |
| Таймаут на `expectItemQuantity` | A/B-вариант степпера | В `CartPage.itemQuantity()` уже два варианта, добавить третий |

---

## Блок 2 · Добить чек-лист cross-cutting (§8 задания)

Задание требует явно подтвердить каждый пункт. Отмечено ☑ в test plan §6 — **проверьте, что вы действительно это делали**, иначе уберите галочку. Врать в чек-листе дороже, чем признать пропуск.

- [ ] Второй браузер (Firefox или Safari) — хотя бы smoke по трём флоу
- [ ] Slow 3G: замерить time-to-first-result на SERP
- [ ] 200 % zoom / крупный шрифт на PDP
- [ ] Мультитаб: изменить qty в табе A → проверить таб B
- [ ] Инкогнито vs залогиненная сессия — сравнить PDP по deep-link
- [ ] Консоль на JS-ошибки в каждом флоу (скрин, если есть)
- [ ] Проверить, что в URL / console / localStorage нет token, адреса, номера карты

Всё, что не сделали — вынести в «Out of scope» с обоснованием, а не оставлять галочку.

---

## Блок 3 · Согласовать документы между собой

- [ ] Test plan §6 (cross-cutting ☑) привести в соответствие с фактом из Блока 2
- [ ] Executive summary: заменить придуманные примеры багов на реальные три находки
- [ ] Executive summary: добавить 1–2 фразы про то, что **две гипотезы не подтвердились** — это сильный сигнал зрелости
- [ ] `README.md`: время — сейчас стоит ~6 ч, поставьте честную цифру
- [ ] Сверить severity/priority в баг-репортах с таблицей определений (§5) — формулировки должны совпадать дословно

---

## Блок 4 · Довести баг-репорты до идеала

- [ ] **BUG-003 (location conflict):** попробовать добить до стабильного репро. Гипотеза — кэш glow-фрагмента. Сценарий: очистить cookies → залогиниться с не-US адресом → **первый** переход сразу на `/s?k=…`. Если поймаете 2/2 — поднимайте severity до High
- [ ] **BUG-001:** записать видео (не только скриншот) — задание прямо просит screen recording для interaction-багов
- [ ] **BUG-002:** приложить HAR или скрин DevTools Network
- [ ] Проверить, что шаги воспроизведения проходит незнакомый человек за < 5 мин (критерий из рубрики §9)
- [ ] Опционально: добавить 4-й баг, если найдёте — максимум 10, но качество > количества

---

## Блок 5 · Приватность и правила (§10 задания)

- [ ] В скриншотах видно «Hello, Anton», ZIP 37217 и содержимое корзины — **замазать или заменить на скрины из инкогнито**
- [ ] Убедиться, что в репозитории нет credentials, `.env`, cookies, HAR с токенами
- [ ] `automation/.gitignore` уже покрывает `node_modules`, `.env`, отчёты — проверить перед коммитом
- [ ] Никакие реальные заказы не оформлялись, платёжные данные не вводились ✔ (тесты останавливаются на auth-гейте)

---

## Блок 6 · Упаковка и сдача (§11)

- [ ] `git init`, коммит, залить в публичный GitHub-репозиторий (или собрать ZIP)
- [ ] Проверить на чистой машине: `git clone` → `cd automation && npm install && npx playwright install chromium && npm test`
- [ ] Корневой `README.md` — точка входа со структурой и оценкой времени
- [ ] Финальная вычитка: severity обоснованы, expected ≠ пересказ title, шаги пронумерованы и минимальны

---

## Приоритет, если времени в обрез

1. **Блок 1** — прогон тестов. Без зелёного прогона бонусная секция обесценивается («runs green» прямо в рубрике §9).
2. **Блок 5** — замазать персональные данные на скринах.
3. **Блок 3** — согласовать exec summary с реальными багами.
4. **Блок 6** — упаковка.

Блоки 2 и 4 — улучшения; если не успеваете, честно перечислите их в разделе «what I'd do with more time».
